{
  "name": "wix-webgl-components",
  "version": "1.0.0",
  "description": "Three.js WebGL Components for Wix Studio - Globe Pointer & Displacement Transition",
  "main": "public/globe-pointer.js",
  "scripts": {
    "build:globe": "esbuild src/globe-pointer.js --bundle --minify --format=iife --outfile=public/globe-pointer.js",
    "dev:globe": "esbuild src/globe-pointer.js --bundle --format=iife --outfile=public/globe-pointer.js --watch",
    "install:displacement": "cd displacement-transition && npm install",
    "build:displacement": "cd displacement-transition && npm install && npm run build && cp public/displacement-transition.js ../public/",
    "dev:displacement": "cd displacement-transition && npm run dev",
    "install:unicorn": "cd unicorn-studio && npm install",
    "build:unicorn": "cd unicorn-studio && npm install && npm run build && cp public/unicorn-studio.js ../public/",
    "dev:unicorn": "cd unicorn-studio && npm run dev",
    "build": "mkdir -p public && npm run build:globe && npm run build:displacement && npm run build:unicorn",
    "install:all": "npm install && cd displacement-transition && npm install && cd ../unicorn-studio && npm install",
    "vercel-build": "npm run install:all && npm run build"
  },
  "keywords": [
    "three.js",
    "wix",
    "custom-element",
    "globe",
    "displacement",
    "scrolltrigger",
    "webgl"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}



